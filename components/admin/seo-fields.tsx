import { MediaPicker } from "@/components/admin/media-picker";
import { SeoPreview } from "@/components/admin/seo-preview";

type SeoFieldsProps = { media: { id: number; filename: string; url: string; width: number; height: number; alt: string }[]; seo?: { metaTitle: string | null; metaDescription: string | null; canonicalUrl: string | null; ogTitle: string | null; ogDescription: string | null; defaultOgImageMediaId: number | null; robotsIndex: boolean; robotsFollow: boolean } };

export function SeoFields({ media, seo }: SeoFieldsProps) {
  return <fieldset className="space-y-5 border-t border-black/10 pt-8"><legend className="text-2xl font-semibold tracking-tight">SEO</legend><p className="text-sm text-black/60">Optional overrides for search engines and social sharing.</p><label className="block text-sm font-medium">SEO title<input className="mt-2 w-full border border-black/20 px-3 py-2 font-normal" defaultValue={seo?.metaTitle ?? ""} name="seoTitle" maxLength={120} /></label><label className="block text-sm font-medium">SEO description<textarea className="mt-2 min-h-24 w-full border border-black/20 px-3 py-2 font-normal" defaultValue={seo?.metaDescription ?? ""} name="seoDescription" maxLength={320} /></label><div className="grid gap-5 md:grid-cols-2"><label className="block text-sm font-medium">Canonical URL<input className="mt-2 w-full border border-black/20 px-3 py-2 font-normal" defaultValue={seo?.canonicalUrl ?? ""} name="canonicalUrl" type="url" /></label><MediaPicker media={media} name="ogImageMediaId" selectedId={seo?.defaultOgImageMediaId} /></div><label className="block text-sm font-medium">Open Graph title<input className="mt-2 w-full border border-black/20 px-3 py-2 font-normal" defaultValue={seo?.ogTitle ?? ""} name="ogTitle" maxLength={120} /></label><label className="block text-sm font-medium">Open Graph description<textarea className="mt-2 min-h-24 w-full border border-black/20 px-3 py-2 font-normal" defaultValue={seo?.ogDescription ?? ""} name="ogDescription" maxLength={320} /></label><div className="flex gap-6"><label className="flex items-center gap-2 text-sm"><input defaultChecked={seo ? !seo.robotsIndex : false} name="noIndex" type="checkbox" />No index</label><label className="flex items-center gap-2 text-sm"><input defaultChecked={seo ? !seo.robotsFollow : false} name="noFollow" type="checkbox" />No follow</label></div><SeoPreview description={seo?.ogDescription || seo?.metaDescription || ""} image={media.find((item) => item.id === seo?.defaultOgImageMediaId)?.url} title={seo?.ogTitle || seo?.metaTitle || ""} url={seo?.canonicalUrl || ""} /></fieldset>;
}